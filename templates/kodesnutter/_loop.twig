{% for post in posts %}
<div class="loop__element">
    <a href="{{ post.url }}">
        <div class="loop__text">
            <div>{{ post.title }}</div>
            <div class="loop__excerpt">{{ post.excerpt }}</div>
        </div>
    </a>
</div>
{% endfor %}